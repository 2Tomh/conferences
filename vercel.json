{
  "buildCommand": "export NODE_OPTIONS='--openssl-legacy-provider' && npm install --legacy-peer-deps && ls -R && npm run build -- --verbose",  "outputDirectory": "dist/ConferenceUI",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}