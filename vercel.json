{
  "buildCommand": "export NODE_OPTIONS='--openssl-legacy-provider' && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}